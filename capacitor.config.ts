import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'looking-menu',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
